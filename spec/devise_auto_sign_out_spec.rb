require "spec_helper"

describe DeviseAutoSignOut do
  it "has a version number" do
    expect(DeviseAutoSignOut::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
